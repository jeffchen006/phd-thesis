## 2026-06-24

- `tex/intro/01-preamble.tex`: removed the visible `\section{Preamble}` heading so the introductory material no longer appears as Section 1.1 in the thesis outline. The original preamble prose is preserved and each paragraph is wrapped in `\revise{...}` so the retained changed content renders in blue.
  - Maps to `notes/0327.md`: "You do not need 1.1"; "1.1 ugly"; "Boolean ending preemable."
- Renamed `tex/intro/01-preamble.tex` to `tex/intro/01-motivation.tex` (and updated the `\input` in `tex/ch-intro.tex`) so the name "preamble" no longer appears anywhere in the thesis source. The file's content (the opening motivation prose) is unchanged.
  - Maps to `notes/0327.md`: removing the "preamble" naming while keeping its content.
- `tex/intro/01-motivation.tex`: restructured the opening so it now (1) formally defines "emerging software systems" as durable, high-stakes programs entrusted with critical assets (funds, keys, credentials, sensitive data) over an extended lifetime that must be maintained/reviewed/protected, explicitly excluding disposable single-use programs; (2) moves the autonomy / vague-natural-language / opaque-LLM material into a paragraph about "untrusted environments" rather than the definition; (3) keeps a short high-stakes consequences paragraph. Also removed the stray "emergent software" wording so the term is consistently "emerging" (matching the title). All new prose is wrapped in `\revise{}` (blue).
  - Maps to the discussion that "emerging software systems" is defined by durability + custody of critical assets (not autonomy), while autonomy belongs to the "untrusted environments" axis of the title.
- `tex/intro/01-motivation.tex`: reframed the "untrusted environments" discussion around a broken assumption. The classical assumption that the entities interacting with a system are humans (e.g., an iOS app expecting people tapping with fingers, behaving at human scale and in good faith) no longer holds: any human-usable interface can also be driven by untrusted autonomous agents that may be malicious, probe for exploits, and operate at machine speed/scale (e.g., a million requests per minute), breaking rate/ordering/good-faith assumptions. A following paragraph keeps the system-internal autonomy + opaque-LLM points. New prose in `\revise{}` (blue). The definition already sits at the very start of the Introduction, before `02-prior-work`, so no reordering was needed.
  - Maps to the "one critical assumption now broken" point: software once assumed human users; now agents/autonomies use it differently, untrusted, with malicious intent and high throughput.
- `tex/intro/01-motivation.tex`: added explicit section headings so the two core concepts are clearly delineated: `\section{Emerging Software Systems}` (1.1) and `\section{Untrusted Environments}` (1.2), plus `\section{Scope of This Dissertation}` (1.3) for the two-families/closing material so it no longer dangles under the Untrusted Environments heading. Titles wrapped in `\revise{}` to render blue. Matches the `\section`-level convention used by the sibling intro files (Prior Work, Contributions, etc.).
  - Maps to the request to "use two clear subsections for explaining these two concepts."
- `tex/intro/01-motivation.tex`: reworked into a single flowing `\section{Motivation}` told as a story with a head and tail. The head (two opening paragraphs) motivates the problem and introduces the two notions; the middle dips into `\subsection{Emerging Software Systems}` and `\subsection{Untrusted Environments}` to define them precisely; the tail (a wrap-up paragraph plus the two-families and common-challenge paragraphs) closes the story and transitions to the rest of the thesis. Dropped the standalone `\section{Scope of This Dissertation}` heading so it is one section, not three. All prose and section/subsection titles wrapped in `\revise{}` (blue).
  - Maps to the request: "make them a whole story... one section with paragraphs... in the middle jump out to a separate subsection to define the two concepts... we need the head and tail."
- `tex/intro/01-motivation.tex`: polished the user's expanded opening draft into five logically connected, plain-but-professional paragraphs following the intended spine: (1) assets up---software manages valuable real-world assets, evidenced by the IMF 2025 Financial Access Survey adoption surge (`\cite{imf2025fas}`); (2) trust down, introducing two aspects; (3) aspect one---who uses the software is now unpredictable (interfaces invoked by autonomous agents, not humans); (4) aspect two---software is built with untrusted LLM coding tools (NL vagueness + model opacity); (5) synthesis---valuable assets guarded by software exposed to autonomous agents and built with untrusted tools, evidenced by the IMF's reported \$12B financial-sector losses (`\cite{financemagnates2024imf}`). Fixed grammar/disfluencies and corrected "large range model" to "large language model." All wrapped in `\revise{}` (blue).
  - Maps to the request to make the selected draft logically fluent and professional in simple CS English.
- `tex/intro/01-motivation.tex`: adopted connector option B (lifecycle / "both ends") for the untrusted-environments side and added the new figure `figures/workflow.pdf`. The roadmap sentence now frames the erosion of trust as appearing "at both ends of a system's lifecycle---when the software is built and when it is used," referencing `Figure~\ref{fig:workflow}`. Inserted the figure (a `figure` float, `\includegraphics[width=\textwidth]`, `\label{fig:workflow}`, and a `\revise{}` caption describing Stage 1 development and Stage 2 deployment). Reordered the two aspect paragraphs to development (Stage 1) then deployment (Stage 2) so the prose follows the figure left-to-right, and tied each paragraph explicitly to its stage ("Stage~1/Stage~2 in Figure~\ref{fig:workflow}"). Also flipped the synthesis paragraph's ordering to build-then-deploy for consistency. Caption wrapped in `\revise{}`.
  - Maps to the request: use connector option B, and add `figures/workflow.pdf` to the motivation with a caption to illustrate the two-stage (development/deployment) idea.
- `main.bib`: removed 14 duplicate (byte-identical) entries that were caused by an accidentally pasted block, which had been breaking the BibTeX step of the build. Removed the second occurrence of each, keeping one copy: albert2020taming, bose2022sailfish, callens2024temporarily, cecchetti2020securing, feist2019slither, grossman2017online, liu2018reguard, luu2016making, ma2021pluto, rodler2018sereum, torres2018osiris, wang2024efficiently, zhang2019mpro, zheng2022park. Verified each key still resolves once and that the full `latexmk` build now completes cleanly (exit 0, no repeated-entry errors, no undefined refs/citations). Not wrapped in `\revise{}` since `.bib` content is not rendered prose.
  - Maps to the request to clear the bibliography file of duplicate entries.
- `tex/intro/01-motivation.tex`: full rewrite of the motivating section into five paragraphs, dropping the "on one hand / on the other hand" framing and moving the workflow figure later (now after paragraph 3, used to illustrate Stage~1 in P2 and Stage~2 in P3). (P1) software increasingly manages valuable real-world assets (`\cite{imf2025fas}`); (P2) the way software is *built* has changed---AI-assisted coding tools lower the barrier and widen the developer base, with examples Claude Code (`\cite{anthropic_claude_code}`), Codex (`\cite{openai_codex_2026}`), OpenCode (`\cite{opencode_2026}`); (P3) *who/what* uses software has changed---programmable interfaces invoked by autonomous agents in unanticipated ways; (P4) resulting security threats: cybercrime is old (`\cite{financemagnates2024imf}`) but amplified, with concrete cases---Moonwell ~\$1.78M loss from an AI-co-authored faulty oracle (`\cite{moonwell2026exploit}`), a state-sponsored group weaponizing an AI coding agent for autonomous espionage (`\cite{anthropic2025espionage}`), and 2025's record >\$3B crypto theft (`\cite{chainalysis2025crypto}`); (P5) informal definition + scope of "emerging software systems" (durable, high-stakes software protecting critical assets---wallets, secrets, credentials, documents; throwaway one-off programs out of scope) and concretization into two domains: Web3 security and LLM harmful-behavior mitigation. This rewrite folds in and replaces the earlier separate head paragraph, the bridge paragraph, the `\subsection{Emerging Software Systems}` / `\subsection{Untrusted Environments}` definitions, and the two-families paragraphs. All prose/caption in `\revise{}` (blue).
  - `main.bib`: added five `@misc` entries for the above (anthropic_claude_code, moonwell2026exploit, anthropic2025espionage, chainalysis2025crypto; codex/opencode already existed). News claims were verified via web search and corrected to accurate facts (Moonwell \$1.78M; Anthropic *disrupted* an attack that *weaponized* Claude Code and banned the accounts, rather than "Anthropic exploited software"). Full `latexmk` build verified clean (exit 0, all citations resolve, figure `fig:workflow` resolves).
  - Maps to the detailed five-paragraph restructure request: list background/assets; software-build changed (AI coding tools, lower barrier, cite Claude Code/Codex/OpenCode, use figure); software-users changed (autonomous agents, use figure); amplified security threats (Moonwell, Anthropic espionage, web3 hacks, cited); informal definition + scope + two thesis domains.
- `tex/intro/01-motivation.tex`: polished the user's own combined-risks paragraph (the "two facts / development stage / deployment stage" paragraph) for grammar and fluency with minimal changes, preserving the user's structure and wording. Fixes included subject-verb agreement, the broken "not only ... but also" construction, "AI-facilitating" -> "AI-assisted", removing redundancy ("no longer apply anymore" -> "no longer apply"), and correcting an inverted-meaning sentence ("made even the expert developers possible to review" / "do have receive enough" -> "makes it difficult even for expert developers to review" / "have not received sufficient"). All in `\revise{}` (blue).
  - Maps to the request to polish that paragraph to be logically fluent and professional in simple CS English.
- `tex/intro/01-motivation.tex`: refined the tone of the motivation to match the user's own writing voice (per the FlashSyn and Trace2Inv introductions: plain, declarative, "However,"-style pivots, concrete dated statistics, few em-dashes/semicolons). Minimal, targeted edits to the two paragraphs originally drafted by the assistant: P4 (security threats) and P5 (definition/scope). Replaced em-dash and semicolon constructions with short declarative sentences and "However,"/"In contrast," pivots; kept all content and citations. Left the user's own paragraphs (P2 build-side, P3 usage-side) unchanged.
  - Maps to the request to refine the motivation using a tone similar to the user's papers, with minimal changes.
- `tex/intro/01-motivation.tex`: two small wording fixes per user approval: P3 "who (or what!) uses it" -> "who, or what, uses it" (removed the informal exclamatory aside to match the paper-intro tone); P2 "limitations of blackbox nature" -> "limitations of their black-box nature".
  - Maps to the two optional notes the user agreed to.
- `main.tex` + `tex/ch-preliminary.tex`: restructured the thesis into Parts (mirroring Nick Feng's thesis structure), reordering so LLM/upstream content precedes smart-contract content, and moved existing content into place. New layout: Part I **Prologue** (Ch 1 Introduction = `ch-intro`, Ch 2 **Background** = `ch-preliminary`, renamed from "Preliminaries and Unified Notation"); Part II **Securing Upstream Development** (Ch 3 *Auditing Malicious Scam Endpoints in LLM-Assisted Code Generation* with Related Work `\input` `related/04-llm-code-security`; Ch 4 *Unfinished Work* with Related Work `related/05-security-RAG`); Part III **Securing Unexpected Usage** (Ch 5 *Securing Smart Contracts with Runtime Invariants* + `related/02-runtime-enforcement`; Ch 6 *Securing Smart Contracts with Control-Flow Integrity* + `related/01-smart-contract-analysis`); Part IV **Epilogue** (Ch 7 *Conclusion and Future Work*, with `related/06-summary-positioning` parked there). The four former empty `\chapter{}` stubs were replaced; the previously-unincluded `tex/related/*.tex` subsection files are now wired in as per-chapter Related Work sections. Chapter bodies left as TODO placeholders per request. Full `latexmk` build verified clean (exit 0); TOC shows the four Parts and seven chapters as intended. Structural `\part`/`\chapter` titles not wrapped in `\revise{}`, matching the existing chapter convention.
  - Maps to the request to adopt Nick Feng's Part-based structure (Prologue / Securing Upstream Development / Securing Unexpected Usage / Epilogue) and move current contents to their corresponding places, leaving unfinished sections empty.
- Renamed Parts II and III and distributed the Background chapter's two sections into per-Part preliminaries. Part II is now **Securing Emerging Software Development**, Part III **Securing Emerging Software Deployment** (user-chosen names). Split `tex/ch-background.tex` (formerly Ch 2 sections 2.1/2.2): the Ethereum/smart-contract section -> new `tex/prelim/deploy-evm-background.tex` (now a `\chapter{Preliminaries}` at the start of Part III), and the LLM-assisted-coding section -> new `tex/prelim/dev-llm-background.tex` (now a `\chapter{Preliminaries}` at the start of Part II). Per the user's confirmed (corrected) mapping: Ethereum background to the smart-contract Part III, LLM background to the LLM Part II. Removed the dangling "the remainder of this chapter introduces the LLM background" sentence from the Ethereum file since the two are now in different Parts. Removed `\input{tex/ch-background}` from `tex/ch-preliminary.tex` and replaced `ch-background.tex` with a pointer comment. Ch 2 (Part I "Background") now holds only the cross-cutting framework/notation (`prelim/01--05`). Full `latexmk` build clean (exit 0); TOC now: Part I (Ch1 Introduction, Ch2 Background) / Part II (Ch3 Preliminaries [LLM], Ch4 Auditing Scam Endpoints, Ch5 Unfinished Work) / Part III (Ch6 Preliminaries [EVM], Ch7 Runtime Invariants, Ch8 Control-Flow Integrity) / Part IV (Ch9 Conclusion).
  - Maps to the request to move background sec. 2.1 and 2.2 into per-Part preliminaries and to improve the Part II/III names.
- Extracted former Section 1.4 ("Completed Work and Remaining Work") out of the thesis into a standalone document. Commented out `\input{tex/intro/05-roadmap-and-organization}` in `tex/ch-intro.tex` (so it is no longer in the thesis; "Outline" becomes the new 1.4). Created root-level `roadmap.tex` (an `article`-class standalone that `\input`s `tex/intro/05-roadmap-and-organization.tex` -> `tex/intro/works-remaining.tex` and uses `\bibliography{main}`). Added a `roadmap` / `roadmap.pdf` target to the `Makefile` (with bibtex), added `roadmap.pdf` to `clean`, and marked phony targets. `make roadmap` builds `roadmap.pdf` with the three Trace2Inv/CrossGuard/Scam2Prompt citations resolved; `make` builds the thesis without the section.
  - Maps to the request to move Section 1.4 (not part of the thesis) into a separate file compiled on its own, with a Makefile update.
- Fixed the actual "cannot compile" cause: several tracked files had been deleted from the working tree, while `tex/ch-preliminary.tex`, `main.tex`, and `tex/intro/02-prior-work.tex` still `\input` them, causing a fatal "File not found" under `pdflatex`. Restored from git: `tex/prelim/01-evm-transition-model.tex`, `02-traces-state-projections.tex`, `03-objectives-and-policies.tex`, `04-layer-instantiations.tex`, `05-notation-policy.tex`, and `tex/related/01-smart-contract-analysis.tex`, `02-runtime-enforcement.tex`, `04-llm-code-security.tex`, `05-security-RAG.tex`. `make` now builds `main.pdf` cleanly with no missing-file errors.
  - Note for the user: if any of those deletions were intentional, the fix is instead to remove the corresponding `\input` lines rather than restore the files.
- Terminology update to match the retitled thesis ("Securing High-Stakes Softwares Against Untrusted Development and Untrusted Use"). Replaced, in context, across `tex/intro/01-motivation.tex`, `tex/intro/03-defense-layers-and-contributions.tex`, `tex/intro/06-outline.tex`, and `tex/related/06-summary-positioning.tex`: "emerging software systems" -> "high-stakes software systems"; "untrusted environments" -> "untrusted development and use" (and the empty `\subsection{Emerging Software Systems}` -> `\subsection{High-Stakes Software Systems}`); dissertation-title quotes -> the new title; "defending"/"secure high-stake" -> "securing"/"secure high-stakes" where it names the thesis theme; the definition sentence de-duplicated ("a high-stakes software system is durable software ..."). Verified no remaining "emerging software system"/"untrusted environment" in the thesis source (sub-papers under tex/from-papers left untouched). `make` builds `main.pdf` cleanly.
  - Maps to the request to update the paper so "emerging software systems", "software system", and "untrusted environment" reflect the new title.
- Moved the LLM-poison report's motivating example into the thesis. Copied the prose/figures from `my-papers/LLM-poison-report/sections/example.tex` into `tex/intro/02-motivating-examples.tex` under `\subsection{Motivating Example I: Financial loss caused by bugs introduced by LLM-coding agents}` (the ChatGPT -> `solanaapis` scam-endpoint incident, with Findings 1 and 2); dropped the source's duplicate `\section{Motivating Example}` heading. Copied the two images `originalTweet.png` and `chat_history.png` from the report's `images/` into `figures/` and repointed `\includegraphics` paths accordingly. Wired the file into `main.tex` right after `\input{tex/intro/01-motivation}` (now \S1.2 "Damage Caused by Untrusted Development and Use", with subsections 1.2.1/1.2.2/1.2.3). Completed the section heading from "Damage Caused by Untrusted" to "...Untrusted Development and Use", and removed the dangling "We provide more discussion ... in Appendix~\ref{sec:appendix_whyscam}" sentence (no such appendix). Copied all 17 needed bib entries from `my-papers/LLM-poison-report/references.bib` into `main.bib` (Vasileva2024AIpoisoning, Binance2024Phishing, shushu2024AIpoisoningBlockBeats, chatgptconversation(+archive), ethereum, solana, pumpfun, victimthread(+archive1/2), github, postman, stackexchange, medium, x_fernandez, solanaapis_net_archive). `make` builds `main.pdf` with 0 undefined citations/refs.
  - Maps to the request to move example.tex into 02-motivating-examples.tex, copy the images to figures/, copy the bib entries, input the file after \S1.1, complete the heading, and remove the appendix sentence.
- Filled in Motivating Example II (\S1.2.2 "Financial loss caused by unexpected usage") from the TxGuard/Trace2Inv paper (`my-papers/TxGuard-report/sections/example.tex`), per the user's choice. Moved the "Exploit Transaction" narrative (the Oct 2020 Harvest Finance oracle-manipulation attack, ~\$33.8M loss via Curve price manipulation) into `tex/intro/motivating_examples.tex` under the Example II subsection. Deliberately left out the Trace2Inv solution material (Abnormal-Behaviors figure, Apply-Inferred-Invariants, Patch-Post-Deployment, RQ1--5) since that belongs in the Trace2Inv chapter, not the intro motivation. Copied the one needed citation `website` (Trace2Inv project page) from `my-papers/TxGuard-report/main.bib` into `main.bib`. `make` builds `main.pdf` with no undefined citations/refs.
  - Maps to the request to move the TxGuard example's motivating content under Example II and copy its references.
- Expanded Motivating Example II to the FULL TxGuard motivating example per the user's confirmed selection (lines 1--232 of `my-papers/TxGuard-report/sections/example.tex`). Replaced the Exploit-Transaction-only version under \S1.2.2 with the complete example: lead-in, Exploit Transaction, Abnormal Behaviors + the four-subfigure statistics figure, outlier analysis, Apply Inferred Invariants, Patch Smart Contracts Post-Deployment, and Research Questions RQ1--5. Adaptations for the thesis: `\Cref{}` -> `Figure~\ref{}` (avoids needing `cleveref`); stripped the paper's internal `\revise{}` revision markers (would render blue); dropped the source's `\section{Motivating Example}\label{sec:example}` (kept under the existing subsection; avoids a duplicate `sec:example` label with Example I) and the commented-out dead blocks. Added `\usepackage{subcaption}` to `main.tex` (for the `subfigure` environments). Copied the four figures `Harvest2_fUSDC_{accessFrequency2,moneyFlow2,totalSupply2,gasConsumption2}.pdf` from `my-papers/TxGuard-report/figures/` into `figures/`. Copied citations `ERC897` and `ERC1167` into `main.bib` (`website` already present). `make` builds `main.pdf` with 0 undefined citations/refs.
  - Maps to the request "move this example to the motivating_examples.tex" (the full selection).
- Rewrote \S1.2 ("Damage Caused by Untrusted Development and Use") of `tex/intro/motivating_examples.tex` into one coherent, escalating story across three motivating examples, with connector sentences and a new section lead-in. (I) Harmful code *generated* by an AI assistant: reframed the ChatGPT incident to emphasize that plain Python exfiltrated the user's private key and the user ran it blindly; folded the systematic URL-poisoning detail into the narrative; single Finding 1. (II) Harmful code that *evades automated review*: the Moonwell vibe-coding incident reframed around development outpacing human review and review being outsourced to AI agents (Copilot + OpenZeppelin) that still missed the bug; condensed the oracle-misconfiguration detail; Finding 2. (III) Trusted code *subverted in use*: the Harvest exploit reframed around expertly written/audited/long-running code being subverted by an unanticipated usage pattern, plus a "defense opportunity" paragraph (a simple runtime check would have blocked it) to set up the thesis's before/after-deployment framing; Finding 3. De-emphasized the all-crypto framing (lessons stated in general software-security terms, facts kept). Renumbered findings to one per example (1/2/3). TRIMMED Example III's RQ1--5 and the verbose "Apply Inferred Invariants"/"Patch Smart Contracts" blocks (Trace2Inv-chapter material) down to the one-line defense-opportunity point. All figures/labels/citations preserved; `make` builds `main.pdf` with no undefined refs/citations. Added `figures/claude.pdf` + `figures/commit_history.png` usage (copied earlier) and `\usepackage{subcaption}`.
  - Maps to the request to make the three motivating examples tell one logically fluent, convincing story, add connectors, allow detail changes, and repivot away from a purely crypto framing.

## 2026-06-25

- `tex/intro/motivating_examples.tex`: shrank the section/subsection headings and renamed the closing subsection, all wrapped in `\revise{}` (blue). Section: "Damage Caused by Untrusted Development and Use" -> "Damage from Untrusted Development and Use". Subsections: "Motivating Example I: Harmful code generated by an AI assistant" -> "Example I: Harmful Code Generated by AI"; "Motivating Example II: Harmful generated code that evades automated review" -> "Example II: Harmful Code Evading Review"; "Motivating Example III: Trusted code subverted by unexpected use" -> "Example III: Trusted Code Subverted in Use"; "Problems Answered in This Dissertation" -> "Problems This Dissertation Addresses".
  - Maps to the request to shorten the section/subsection titles and rename the last subsection to "Problems This Dissertation Addresses".
- `tex/intro/motivating_examples.tex`: kept Figure~1.6 (the four-panel Harvest statistics figure, `fig:harvest_abnormal`) on the same page as Section 1.2.3 ("Example III: Trusted Code Subverted in Use"). Moved the `figure` environment from after the section's body text to immediately after the `\subsection` heading, and changed its float placement from `[!htbp]` to `[t]` so it floats to the top of the page where the section begins. Verified via `latexmk`: both the 1.2.3 heading and `fig:harvest_abnormal` (Figure 1.6) now resolve to page 8 (previously the figure floated to page 9). Layout-only change, no prose altered (nothing to wrap in `\revise{}`).
  - Maps to the request "I want Figure 1.6 to be in the same page as Section 1.2.3".
- `tex/intro/motivation.tex`: removed all formal apparatus from the motivation and shrank the informal explanations, to match how the committee-approved reference theses (Feng 2025, Grubb 2019) handle their introductions (prose + motivating examples only; no `Definition` boxes or set-theoretic notation in Chapter 1). Deleted the three `\begin{definition}` environments (`def:high-stakes`, `def:untrusted-dev`, `def:untrusted-use`) and the formal-notation paragraph (the $S$ / $\Phi(A)$ / $\mathcal{I}_{\mathrm{dev}}$ / $\mathcal{I}_{\mathrm{use}}$ "More formally, let ..." paragraph). Condensed the three subsections (High-Stakes Software, Untrusted Development, Untrusted Use) into short informal prose that keeps each concept but drops the redundant "This definition covers..."/"The defining feature..." follow-up paragraphs. Fixed a stale cross-reference: "The Harvest example in Chapter~1 illustrates this" -> "as the Harvest incident in this chapter illustrates" (the example is §1.2.3, after this §1.1 subsection, so the old "above" framing was wrong). Verified no remaining references to the three deleted `def:*` labels anywhere in the source. New/rewritten prose wrapped in `\revise{}` (blue). `make` builds `main.pdf` (33 pages) with 0 undefined refs/citations.
  - Maps to `notes/0327.md`: "Terminology: make people read annoyed. Informal explanation. Enable the outsider... Suppose to be read by the public" -- and the user's request to remove the formal definitions and shrink the informal explanations, following how Feng's and Grubb's theses keep formal definitions out of the introduction.
- `tex/intro/motivation.tex`: replaced the "two domains" scope sentence ("We concretize this goal in two domains: the security of Web3 software, and the mitigation of harmful behavior in LLM-based systems.") with a cryptocurrency-as-concrete-stakes framing, to strengthen the blockchain tie the committee asked for (`notes/0327.md`: "Obvious ties to blockchain side"). New text names cryptocurrencies as the most direct stakes (theft = immediate, measurable financial loss, unlike credentials/data), then frames the thesis as following the build workflow and defending each stage against untrusted factors. Polished the user's supplied draft for grammar/fluency (fixed "is cryptocurrencies", "defind", "softwares"; tightened wording). Edit sits inside the existing `\revise{}` block, so it renders blue. `make` builds `main.pdf` (33 pages) with 0 undefined refs/citations.
  - Maps to the user's request to swap the two-domains sentence for the cryptocurrency-stakes framing (assistant allowed to reword), and to `notes/0327.md` "obvious ties to blockchain side".
- `tex/intro/motivation.tex` + `main.bib`: added four citations supporting the claim that developers routinely rely on LLMs/AI-assisted coding tools (P2 of the Motivation). Cited after "tools to help implement software", wrapped as `\revise{~\cite{...}}` so the citation marker renders blue. Added bib entries (verified via web search for accurate venue/author/DOI): `liang2024largescale` (Liang, Yang, Myers, "A Large-Scale Survey on the Usability of AI Programming Assistants: Successes and Challenges", ICSE 2024, doi 10.1145/3597503.3608128); `barke2023grounded` (Barke, James, Polikarpova, "Grounded Copilot: How Programmers Interact with Code-Generating Models", PACMPL/OOPSLA 2023, distinguished paper, doi 10.1145/3586030); `hou2024llm4se` (Hou et al., "Large Language Models for Software Engineering: A Systematic Literature Review", ACM TOSEM 33(8), 2024, doi 10.1145/3695988); `stackoverflow2024survey` (Stack Overflow 2024 Developer Survey -- 62% of professional developers report currently using AI tools). Three peer-reviewed (ICSE/OOPSLA/TOSEM) plus one large industry adoption survey for the usage rate. `bibtex`+`make` build clean; all four resolve in `main.bbl`, 0 undefined citations.
  - Maps to the user's request for citations from top conferences/journals supporting the "developers routinely rely on LLMs and AI-assisted coding tools" claim.
- `tex/intro/motivation.tex`: replaced the `XXX` placeholder (P2) with concrete study facts drawn from the two papers, cited separately rather than as one grouped citation. From Liang et al. (ICSE 2024): a survey of 410 developers who turn to AI assistants mainly to write code faster, reduce keystrokes, and recall syntax~\cite{liang2024largescale}. From Barke et al. (OOPSLA 2023): a close study of 20 programmers across four languages who used GitHub Copilot in two recurring modes -- accelerating tasks they already knew, and exploring unfamiliar APIs/solutions~\cite{barke2023grounded}. Used verified figures (sample sizes 410 and 20, and each paper's headline finding) rather than invented percentages, since exact adoption percentages were not cleanly extractable from the sources. New sentence wrapped in `\revise{}` (blue). `make` builds `main.pdf` (33 pages) with 0 undefined refs/citations.
  - Maps to the user's request to replace XXX with statistics from the two papers and cite/refer to them separately.
- `tex/intro/motivation.tex` + `main.bib`: polished the P3 "untrusted use" paragraph (the rocket/iOS opener through the four usage examples), fixed grammar (IOS -> iOS, "softwares" -> software, "people uses" -> "people use", "Another examples" -> ...), refined the opening sentence into a connector ("The same forces that are reshaping how software is built are also reshaping who, or what, ultimately uses it"), and backed every example with a dated, sourced statistic found via web search. Added bib entries: `imperva2024badbot` (Imperva 2024 Bad Bot Report -- bots ~50% of internet traffic in 2024, surpassing humans for the first time, ~20% malicious); `plesner2024recaptcha` (Plesner, Vontobel, Wattenhofer, "Breaking reCAPTCHAv2", COMPSAC 2024 / arXiv:2409.08831 -- 100% solve rate vs 68--71% prior); `he2024fakestars` (He et al., "Six Million (Suspected) Fake Stars in GitHub", arXiv:2412.13459 -- millions of fake stars, surged 2024); `layerzero2024airdrop` (The Defiant -- LayerZero June 2024 ZRO airdrop flagged 800,000+ Sybil wallets, ~13% of eligible). NOTE: the user's GitHub example (faking commits to boost a resume) is only supported by blog posts, so it was reframed to the rigorously measured version -- automated accounts inflating GitHub project reputation via fake stars (He et al.). Entire rewritten paragraph wrapped in `\revise{}` (blue). `bibtex`+`make` build clean (34 pages, 0 undefined refs/citations).
  - Maps to the user's request to polish the selected paragraph, add references for the given examples, search for the latest examples, and add detailed statistics and dates.
- `tex/intro/motivation.tex`: reworded the "The above two facts..." sentence -- the two preceding paragraphs describe *changes*, not facts. Replaced with "These two shifts---in how software is built and in how it is used---bring clear convenience, but they also introduce new risks beyond those of the traditional software-development workflow." ("shifts" also matches the later "Because of these shifts" / "the two changes above" wording.) Wrapped in `\revise{}` (blue). `make` builds clean (34 pages, 0 undefined refs).
  - Maps to the user's request to improve the sentence and fix that "the above two" are not "facts".
- `tex/intro/motivation.tex`: polished the forward-reference sentence in the development-stage paragraph. "A later section~\ref{subsec:example2} presents a real-world example of how a single line of code generated by an AI coding tool can cause millions of dollars in losses, even when the code was reviewed and approved by multiple professional AI reviewing tools and human developers." -> "Section~\ref{subsec:example2} later presents a real-world case in which a single faulty line of AI-generated code caused millions of dollars in losses, even after passing review by both professional AI code-review tools and human developers." (tightened the lead-in, "AI reviewing tools" -> "AI code-review tools", past tense for the concrete incident). Verified `subsec:example2` resolves to the Example II subsection. Wrapped in `\revise{}` (blue). `make` builds clean (34 pages, 0 undefined refs).
  - Maps to the user's request to polish the selected lines.
- `tex/intro/motivation.tex`: polished the deployment-stage ("Second, ...") paragraph. Removed the triple "the software ... the software ... the softwares" repetition ("when monitoring the software's usage and maintaining the software and patching the software accordingly, the softwares are facing an unprecedented new challenge" -> "the tasks of monitoring usage, maintaining the system, and patching it accordingly face an unprecedented challenge"); dropped redundant "unprecedented new"; fixed "day-night" -> "day--night" (en-dash, matching house style); tightened the human-vs-agent contrast; and polished the forward reference ("a hacker exploits a software in blockchain in a dramatically different way than all other users" -> "an attacker exploited a blockchain application in a way dramatically different from every other user"), with past tense for the concrete incident. Verified `subsec:example3` resolves to the Example III subsection. Wrapped in `\revise{}` (blue). `make` builds clean (34 pages, 0 undefined refs).
  - Maps to the user's request to polish the selected lines (deployment-stage paragraph).
- `tex/intro/motivation.tex`: final grammar-only pass over the whole Motivation section (no rewording beyond fixes). Fixes: "Softwares are built" -> "Software is built" (+ Oxford comma "store, manage, and operate"); "large language models(LLMs)" -> "...models (LLMs)" (spacing); "other softwares" -> "other software"; "6 millions of fake GitHub stars" -> "6 million fake GitHub stars"; in the Example I sentence "sent user's private key" -> "sent the user's private key", added comma after "scam server", "more then" -> "more than", and tense "the user's prompts are" -> "were"; and fixed a run-on in the Untrusted Use paragraph ("...for example who calls the system, in what order, at what scale, and these assumptions may hold" -> "...for example, who calls the system, in what order, and at what scale. These assumptions may hold"). Each corrected fragment wrapped in `\revise{}` (blue); spacing-only fix not wrapped (nothing visible). `make` builds clean (34 pages, 0 undefined refs).
  - Maps to the user's request for one last grammar-only polish of the Motivation section.
- `tex/intro/motivation.tex`: refined the closing thesis-statement paragraph of the Motivation. Removed the repeated "untrusted factors" and the awkward "defend against the untrusted factors that introduce them"; replaced "ask, at each stage, which vulnerabilities and harms could let an attacker steal these assets, and we develop methods to defend against..." with "Following the lifecycle of such systems, we examine, at each stage, how these factors can be exploited to compromise the assets the software guards, and we build defenses that contain this risk both before and after deployment." Ends on the "before and after deployment" framing used by the workflow figure (Stage 1 / Stage 2) and the rest of the thesis. Wrapped in `\revise{}` (blue). Full clean build verified (34 pages, 0 undefined refs).
  - Maps to the user's request to polish/refine the last paragraph of the Motivation, which they felt was not good enough.
- `tex/intro/motivation.tex`: made the two concretizations explicit and parallel in the Untrusted Development / Untrusted Use subsections. Added a closing sentence to Untrusted Development: "In this dissertation, we concretize untrusted development as the use of \emph{AI-assisted coding tools}, today the most pervasive and least-vetted such influence...". Rewrote the smart-contracts sentence in Untrusted Use to match the same "we concretize untrusted use as ..." framing and fixed its broken parallel list ("(1) high-stakes, because they guard digital assets, and (2) allowing any blockchain user to invoke them" -> "they are high-stakes, since they directly guard digital assets, and they are open to invocation by any blockchain user"). Both wrapped in `\revise{}` (blue). Clean build (34 pages, 0 undefined refs).
  - Maps to the user's request to emphasize that the thesis concretizes untrusted development into AI-assisted coding tools and untrusted use into smart contracts.
- `tex/intro/motivating_examples.tex` (\S1.2.4 "Problems This Dissertation Addresses"): rewrote the malformed `itemize` of sub-problems (the list had no `\item`s and garbled placeholder prose, e.g. "injecting malicious intentionally adult content...") into four clean, parallel `\item`s, all wrapped in `\revise{}` (blue). The four now map one-to-one onto the dissertation's contributions/chapters: (1) **Harmful code generated during development** -> Ch 2 auditing scam endpoints in production LLMs; (2) **Poisoned external content during development** -> Ch 3 defending coding agents against search-/documentation-level poisoning; (3) **Unanticipated use after deployment** -> Ch 4 runtime invariants; (4) **Constraining behavior at runtime** -> Ch 5 control-flow integrity. Each item is a single polished question with a bold lead-in, matching the tone of the `\textbf{Central question.}` item above it. Also added a lead-in clause ("...into four more specific problems, two on each side of deployment.") and wrapped the closing "Answering these questions..." paragraph in `\revise{}`. Verified the four match `contributions.tex`. `latexmk` builds `main.pdf` cleanly (exit 0, no undefined refs).
  - Maps to the request to polish/refine the selected sub-problem list using context from the rest of the thesis.
